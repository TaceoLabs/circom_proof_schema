# Circom Proof Schema

This crate is a helper crate that allows to simulate a ceremony for a Groth16 trusted setup from a circom circuit.

Note that this is very convenient for testing, but a real deployment should always use a real ceremony!
